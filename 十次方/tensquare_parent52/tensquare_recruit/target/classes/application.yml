server:
  #服务端口
  port: 9002
spring:
  application:
    #指定服务名
    name: tensquare-recruit
  #数据源配置
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.139.167:3306/tensquare_recruit?useSSL=false&serverTimezone=UTC
    username: root
    password: root
  #JPA的整合配置
  jpa:
    database: mysql
    show-sql: true
